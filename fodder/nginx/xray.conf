# ========= WS helper (letak dalam http context; conf.d biasanya memang dalam http) =========
map $http_upgrade $connection_upgrade {
  default upgrade;
  ''      close;
}

# =========================
# WS MULTIPLEX (1010)
# - VLESS guna path "/"
# - VMESS "/vmess"
# - TROJAN "/trojan-ws"
# - SS "/ss-ws"
# - SSH: fallback untuk semua path lain (dan juga /fightertunnelssh)
# =========================
server {
  listen 1010 proxy_protocol so_keepalive=on reuseport;
  server_name z.zydnet.fun;

  # kalau proxy_protocol datang dari IP lain (HAProxy/ALB), tambah IP dia sini
  set_real_ip_from 127.0.0.1;
  real_ip_header proxy_protocol;

  # --- VLESS WS: PATH ROOT "/" ---
  location = / {
    proxy_http_version 1.1;
    proxy_set_header Upgrade $http_upgrade;
    proxy_set_header Connection $connection_upgrade;

    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;

    proxy_read_timeout 86400;
    proxy_send_timeout 86400;

    proxy_pass http://127.0.0.1:10001;
  }

  # --- VMESS WS ---
  location = /vmess {
    proxy_http_version 1.1;
    proxy_set_header Upgrade $http_upgrade;
    proxy_set_header Connection $connection_upgrade;

    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;

    proxy_read_timeout 86400;
    proxy_send_timeout 86400;

    proxy_pass http://127.0.0.1:10002;
  }

  # --- TROJAN WS ---
  location = /trojan-ws {
    proxy_http_version 1.1;
    proxy_set_header Upgrade $http_upgrade;
    proxy_set_header Connection $connection_upgrade;

    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;

    proxy_read_timeout 86400;
    proxy_send_timeout 86400;

    proxy_pass http://127.0.0.1:10003;
  }

  # --- SS WS ---
  location = /ss-ws {
    proxy_http_version 1.1;
    proxy_set_header Upgrade $http_upgrade;
    proxy_set_header Connection $connection_upgrade;

    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;

    proxy_read_timeout 86400;
    proxy_send_timeout 86400;

    proxy_pass http://127.0.0.1:10004;
  }

  # --- SSH WS (path rasmi) ---
  location = /fightertunnelssh {
    proxy_http_version 1.1;
    proxy_set_header Upgrade $http_upgrade;
    proxy_set_header Connection $connection_upgrade;

    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;

    proxy_read_timeout 86400;
    proxy_send_timeout 86400;

    proxy_pass http://127.0.0.1:10015;
  }

  # --- SSH WS fallback: selain path yang atas, pergi SSH ---
  # (ini buat SSH kau “confirm work” walaupun user pakai path lain,
  #  tapi root "/" tetap reserved untuk VLESS)
  location / {
    proxy_http_version 1.1;
    proxy_set_header Upgrade $http_upgrade;
    proxy_set_header Connection $connection_upgrade;

    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;

    proxy_read_timeout 86400;
    proxy_send_timeout 86400;

    # normalize semua jadi 1 path ke backend SSH
    rewrite ^ /fightertunnelssh break;

    proxy_pass http://127.0.0.1:10015;
  }
}

# =========================
# OVPN/WS (1012)
# =========================
server {
  listen 1012 proxy_protocol so_keepalive=on reuseport;
  server_name xxx;

  set_real_ip_from 127.0.0.1;
  real_ip_header proxy_protocol;

  location / {
    proxy_http_version 1.1;
    proxy_set_header Upgrade $http_upgrade;
    proxy_set_header Connection $connection_upgrade;

    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;

    proxy_read_timeout 86400;
    proxy_send_timeout 86400;

    rewrite ^ /fightertunnelovpn break;
    proxy_pass http://127.0.0.1:10012;
  }
}

# =========================
# HTTPS static site (81)
# =========================
server {
  listen 81 ssl http2 reuseport;
  server_name xxx;

  ssl_certificate     /etc/xray/xray.crt;
  ssl_certificate_key /etc/xray/xray.key;

  ssl_protocols TLSv1.2 TLSv1.3;
  ssl_ciphers EECDH+CHACHA20:EECDH+CHACHA20-draft:EECDH+ECDSA+AES128:EECDH+aRSA+AES128:RSA+AES128:EECDH+ECDSA+AES256:EECDH+aRSA+AES256:RSA+AES256:EECDH+ECDSA+3DES:EECDH+aRSA+3DES:RSA+3DES:!MD5;

  root /var/www/html;
  index index.html index.htm;
}

# =========================
# gRPC (1013) - guna prefix sebab gRPC path biasanya /serviceName/Method
# =========================
server {
  listen 1013 http2 proxy_protocol so_keepalive=on reuseport;
  server_name xxx;

  set_real_ip_from 127.0.0.1;
  real_ip_header proxy_protocol;

  location ^~ /vless-grpc {
    grpc_set_header Host $host;
    grpc_set_header X-Real-IP $remote_addr;
    grpc_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    grpc_read_timeout 86400;
    grpc_send_timeout 86400;
    grpc_pass grpc://127.0.0.1:10005;
  }

  location ^~ /vmess-grpc {
    grpc_set_header Host $host;
    grpc_set_header X-Real-IP $remote_addr;
    grpc_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    grpc_read_timeout 86400;
    grpc_send_timeout 86400;
    grpc_pass grpc://127.0.0.1:10006;
  }

  location ^~ /trojan-grpc {
    grpc_set_header Host $host;
    grpc_set_header X-Real-IP $remote_addr;
    grpc_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    grpc_read_timeout 86400;
    grpc_send_timeout 86400;
    grpc_pass grpc://127.0.0.1:10007;
  }

  location ^~ /ss-grpc {
    grpc_set_header Host $host;
    grpc_set_header X-Real-IP $remote_addr;
    grpc_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    grpc_read_timeout 86400;
    grpc_send_timeout 86400;
    grpc_pass grpc://127.0.0.1:10008;
  }
}
